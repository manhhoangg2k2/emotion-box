import 'package:flutter/material.dart';

class AppShadow {
  static final boxShadow = [
    const BoxShadow(
      color: Color(0x1A000000),
      offset: Offset(2, 2),
      blurRadius: 10,
      spreadRadius: 0,
    ),
  ];
}
